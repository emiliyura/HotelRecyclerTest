<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark theme variant -->
    <style name="Theme.HotelRecyclerTest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/lavender</item>
        <item name="colorPrimaryVariant">@color/purple_200</item>
        <item name="colorOnPrimary">@color/black</item>

        <!-- Secondary colors -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Background and surface colors -->
        <item name="android:windowBackground">@color/black</item>
        <item name="colorSurface">@color/black</item>

        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/purple_200</item>
    </style>

    <!-- Explicit dark theme -->
    <style name="AppTheme_Dark" parent="Theme.HotelRecyclerTest">
        <!-- Можно добавить специфичные для темной темы переопределения -->
    </style>
</resources>